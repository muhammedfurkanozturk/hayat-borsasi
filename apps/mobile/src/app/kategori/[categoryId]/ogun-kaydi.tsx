import { ComingSoonScreen } from "@/components/coming-soon-screen";

// Web'in AI fotoğraf analizi + USDA arama + sürükle-bırak öğün planlayıcısı
// — mobilde henüz yok, ayrı bir turda ele alınmalı (bkz. CLAUDE.md bölüm 9).
export default function OgunKaydiTab() {
  return <ComingSoonScreen label="Öğün Kaydı" icon="silverware-fork-knife" />;
}
