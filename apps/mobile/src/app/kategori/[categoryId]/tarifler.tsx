import { ComingSoonScreen } from "@/components/coming-soon-screen";

// Web'in AI tarif üretimi (3 modlu sistem) — mobilde henüz yok, ayrı bir
// turda ele alınmalı (bkz. CLAUDE.md bölüm 9).
export default function TariflerTab() {
  return <ComingSoonScreen label="Tarifler" icon="book-open-variant" />;
}
